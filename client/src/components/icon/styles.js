import styled from 'styled-components';
import { MEASUREMENTS } from '../../constants/measurements';

const StyledIcon = styled.img`
	height: ${MEASUREMENTS.ICONS.HEIGHT};
	width: ${MEASUREMENTS.ICONS.WIDTH};
`;

export { StyledIcon };
